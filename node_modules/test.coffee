# util = require('util')
# _ = require('./underscore')

# a = []
# b = []

# array_equal = (a, b) ->
# 	_.all(_.zip(a, b), (k) -> k[0] == k[1] )

# util.puts array_equal(a, b)

PortNegotiator = require('./portnegotiator').PortNegotiator

pn = new PortNegotiator()

pn.on 'attach', (device) ->
pn.on 'detach', (device) ->
pn.on 'choice', (devices) ->
