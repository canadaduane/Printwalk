(function() {
  var PortNegotiator, pn;

  PortNegotiator = require('./portnegotiator').PortNegotiator;

  pn = new PortNegotiator();

  pn.on('attach', function(device) {});

  pn.on('detach', function(device) {});

  pn.on('choice', function(devices) {});

}).call(this);
